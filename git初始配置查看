#查看git版本
git --version 

#git连接github
ssh-keygen -t rsa -C "kobe-mop@qq.com"
vi /c/Users/Administrator/.ssh/id_rsa.pub
在生成的id_rsa.pub，将里面的内容复制，进入你的github账号，在settings下，SSH and GPG keys下new SSH key，然后将id_rsa.pub里的内容复制到Key中，完成后Add SSH Key。


#查看是否连接成功
ssh git@github.com

#配置
git config --global user.name asb
git config --global user.email kobe-mop@qq.com
git config --global user.ui True

#查看配置
git config --list
cd ~
vi .gitconfig
